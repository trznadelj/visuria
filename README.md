# visuria


Browser-based signal and ML visualization tool. Drag-and-drop interface with tensor operations, FFT (Cooley-Tukey + naive), and some neural network inference. 

Open `index.html` in a browser — no build step, no server needed.

Initial commits, not really useful for anything yet. But - getting usable to show OFDM signals.
