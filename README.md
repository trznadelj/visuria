# visuria

Browser-based ML and signal visualization tool. Drag-and-drop interface with tensor operations, FFT (Cooley-Tukey + naive), and neural network inference.

Open `index.html` in a browser — no build step, no server needed.

Initial commit, not really useful for anything yet.
